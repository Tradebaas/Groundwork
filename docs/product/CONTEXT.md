# CONTEXT: domain glossary

<!-- TEMPLATE: seeded during `begin`, grown by `scope` and `spec` the moment a fuzzy word
     surfaces. This is the project's shared vocabulary; it compresses tokens session after
     session because agents read one line here instead of re-deriving what a word means. Rules:

     - Project-specific terms only. No general programming concepts.
     - One entry per term: term in bold, a one-to-two-line definition, then an Avoid list of
       banned synonyms. Use the term everywhere; never its synonyms.
     - Definitions cover language, nothing else. No implementation detail in this file.
     - Update inline, the moment a term is settled. Never batch glossary edits for later.

     Worked example (delete when the first real term lands):

     - **Customer** - the paying business account that owns projects and receives invoices.
       One Customer has many Users. Avoid: account, client, tenant.
-->

- TBD
